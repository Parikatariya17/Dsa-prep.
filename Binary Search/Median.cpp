#include <iostream>
#include <vector>
#include <tuple>
#include <algorithm>
using namespace std;

void Merge(int n1,vector<int>arr1,int n2,vector<int>arr2){
        int i = 0,j = 0;
        
        while(i<n1&&j<n2){

        }
}

void Merging(int n1,vector<int>arr1,int n2,vector<int>arr2){
        Merge(n1,arr1,n2narr2);
}

int main(){
    vector<int> arr1 = {2, 12, 11, 3, 26, 7},arr2={};
    Merging(arr1.size(),arr1,arr2.size(),arr2);
}